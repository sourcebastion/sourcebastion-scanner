# v1.7.31 release evidence

**Status:** HOLD — candidate reviewed technically; independent approval and the
protected release execution are still outstanding.

This record separates evidence already observed from evidence that must come
from the release run. Do not mark a gate complete from a configured workflow,
a successful pull-request build, or an authenticated registry pull alone.

## Candidate evidence

Observed 2026-09-22:

- Release PR: <https://github.com/sourcebastion/sourcebastion-scanner/pull/17>
- Candidate head: `96004812b772fca40896ca797944681bc03b0b06`
- Declared version: `1.7.31`
- Pull-request checks: all configured required checks passed on the candidate
  head, including all five image builds, unit/security/lint checks, CodeQL,
  dependency review, VS Code extension tests, and both scanner workflows.
- Independent review: outstanding (`REVIEW_REQUIRED`).
- Published GitHub release: absent.

Those observations prove the candidate is buildable. They do not prove the
protected `Release` workflow, immutable promotion, public package visibility,
attestations, or anonymous pull.

## Release execution record

Fill this section from one successful workflow run after PR #17 is independently
approved and merged. Preserve full commit IDs and OCI digests; do not record a
moving compatibility tag as release identity.

| Evidence | Required value |
| --- | --- |
| Release approver | Different named person from the dispatcher |
| Release workflow URL | Successful `Release` run dispatched from `main` |
| Release commit | Full `main` commit ID containing `VERSION=1.7.31` |
| GitHub release | Published, non-draft `v1.7.31` |
| Standard digest | `ghcr.io/sourcebastion/sourcebastion-scanner@sha256:...` |
| Slim digest | Full `sha256:...` digest |
| Micro digest | Full `sha256:...` digest |
| Thin digest | Full `sha256:...` digest |
| Semgrep digest | Full `sha256:...` digest |
| Provenance | Successful verification for every digest |
| SBOM | Present for every release image |
| Anonymous pull | Standard digest pulled with an empty Docker config |

## Verification procedure

1. Confirm PR #17 has an independent approval and is merged. From the merged
   `main` commit, dispatch `Release` with the exact input `v1.7.31`.
2. Require a second named authority to approve the protected `release`
   environment. The dispatcher must not approve their own release.
3. Wait for every release job to succeed. Record the workflow URL, commit, and
   five output digests before evaluating compatibility tags.
4. Verify each digest's GitHub build provenance:

   ```bash
   gh attestation verify \
     oci://ghcr.io/sourcebastion/sourcebastion-scanner@sha256:<digest> \
     --repo sourcebastion/sourcebastion-scanner
   ```

5. Verify the package is public without reusing local registry credentials:

   ```bash
   anonymous_docker_config="$(mktemp -d)"
   DOCKER_CONFIG="$anonymous_docker_config" docker pull \
     ghcr.io/sourcebastion/sourcebastion-scanner@sha256:<standard-digest>
   rm -rf -- "$anonymous_docker_config"
   ```

6. Confirm `v1.7.31` is published rather than draft, targets the recorded
   commit, and exposes the expected release metadata. Confirm the full-version
   and full-commit tags resolve to the recorded immutable digests.

If any job, attestation, digest comparison, public pull, or second-person gate
fails, leave this record on HOLD. Do not move a full-version tag or substitute a
successful pull-request image for release evidence.
